import { PropertyLoadingAnimation } from '@/components/ui/PropertyLoadingAnimation';

export default function Loading() {
    return <PropertyLoadingAnimation propertyType="kiro" />; // Default to a type, or handle generic
}
